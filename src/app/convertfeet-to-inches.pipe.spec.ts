import { ConvertfeetToInchesPipe } from './convertfeet-to-inches.pipe';

describe('ConvertfeetToInchesPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertfeetToInchesPipe();
    expect(pipe).toBeTruthy();
  });
});
