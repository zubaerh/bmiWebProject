import { BmiResultPipe } from './bmi-result.pipe';

describe('BmiResultPipe', () => {
  it('create an instance', () => {
    const pipe = new BmiResultPipe();
    expect(pipe).toBeTruthy();
  });
});
